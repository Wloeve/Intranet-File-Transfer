# 更新日志

本项目所有重要变更记录于此。
版本号遵循语义化版本（主版本.次版本.修订号），条目格式参考 Keep a Changelog。

## [3.5.1] - 2026-09-18

### 修复

- **下载稳定性**：发送循环改为裸 `socket.send` + 字节级计数，卡死时在原位置重试
  （连续 4 次、约 8 分钟无进展才放弃），Wi-Fi 瞬断 / iPad 锁屏暂停不再直接断连
- **续传正确性**：新增 `ETag` / `Last-Modified` 响应头并正确处理 `If-Range`，
  文件变更后续传不会把新旧内容拼接成损坏文件

### 新增

- `server.log` 记录每次下载中断的位置（`下载中断 xxx 已发 n/m 字节`），便于排查

## [3.5.0] - 2026-09-18

### 新增

- 独立 exe 分发：`build_exe.bat` 一键打包（PyInstaller onefile），
  生成的单文件 exe 在任意 Windows 10/11 电脑免装 Python 运行
- 源码支持 frozen 模式：exe 运行时以 exe 所在目录为根目录
- 启动脚本兼容任意 Windows 11：跳过 Microsoft Store 假 `python` 命令
  （WindowsApps 目录检测），按 py 启动器 → PATH → 常见安装目录顺序搜索并校验版本 ≥ 3.7
- 启动时对保存目录做写入探针，放在 `Program Files` 等只读位置时给出明确中文提示

### 变更

- 入口脚本改为 ASCII 文件名：`start_server.bat` / `build_exe.bat`

## [3.4.3] - 2026-09

### 修复

- 端口占用检测：避免多实例同时运行导致请求被随机分发

## [3.4.0] - 2026-09

### 新增

- 1~4 条 WebSocket 并行上传，绕开单条 TCP 流的窗口限制
- 逐条看门狗、故障隔离、自动降级到 1 条连接的防退化设计

## [3.3.0] - 2026-09

### 新增

- WebSocket 上传主通道：整文件只用一条长连接，
  彻底消除「每片一次 HTTP 请求」的握手开销与卡死点；HTTP 分片接口保留为兜底

## [3.2.0] - 2026-09

### 修复

- iPad Safari 上传大文件「传一小段就静默卡死」：
  API 一律不复用连接（避免复用被 Wi-Fi 抖动 / NAT 悄悄断掉的长连接），
  并加入两级停滞自愈机制

## [3.0.0] - 2026-09

- 首个公开版本：零依赖单文件服务，支持 iPad / 手机与电脑互传大文件（20G+），
  含多分片并发、断点续传、下载断点续传与实时速率显示
